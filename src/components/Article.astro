---
const { title, description, image, calendly, href } = Astro.props;
---

<article class='flex gap-8 items-center flex-col md:flex-row'>
  <div class='w-full md:w-64 shadow-lg rounded-lg'>
    <img class='w-full md:w-64 rounded-lg' src={image} alt={title} />
  </div>
  <div class='md:w-2/3 flex flex-col p-4'>
    <h4 class='font-semibold text-xl text-center md:text-start mb-3'>
      {title}
    </h4>
    <p class='lg:text-base mb-4'>
      {description.slice(0, 190)}
    </p>
    <div
      class='flex flex-col items-center justify-start md:flex-row gap-2 text-base'
    >
      <a
        href={href}
        class='border border-blue-600 hover:text-white hover:bg-blue-600 hover:font-semibold px-6 py-2 rounded-full text-blue-600'
        >Leer mas</a
      >
      <a
        href={calendly}
        type='button'
        class='mt-4 md:mt-0 border border-blue-600 hover:text-white hover:bg-blue-600 hover:font-semibold px-6 py-2 rounded-full text-blue-600'
        >Agendar una cita</a
      >
    </div>
  </div>
</article>
